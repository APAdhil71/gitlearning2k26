configuaration
==============
>git config -- list [list al config details]
>git config -- global user.name "username" [adding a global user.name]
>git config -- global user.name "useremail@gmail.com" [adding a global user.email]
command for initaliazing git repository:
>git init
command for adding all changes to staging area
>git add-A 
command for adding staging files to local repository
>git commit -m "commit message"
command for listing all untracked files
>git status
command for list branch
>git branch
command for view the remote repositories
> git remote -v
This command is used to connect your local Git repository to a remote repositor
>git remote add origin <repo-url>  

